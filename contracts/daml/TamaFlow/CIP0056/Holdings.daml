module TamaFlow.CIP0056.Holdings where

import DA.Time (RelTime)

import TamaFlow.CIP0056.TokenMetadata

data InstrumentId = InstrumentId
  with
    admin : Party
    id : Text
  deriving (Eq, Show)

data Lock = Lock
  with
    holders : [Party]
    expiresAt : Optional Time
    expiresAfter : Optional RelTime
    context : Optional Text
  deriving (Eq, Show)

data HoldingView = HoldingView
  with
    owner : Party
    instrumentId : InstrumentId
    amount : Decimal
    lock : Optional Lock
    meta : Metadata
  deriving (Eq, Show)

interface Holding where
  viewtype HoldingView

instrumentMatches : InstrumentId -> InstrumentId -> Bool
instrumentMatches left right =
  left.admin == right.admin && left.id == right.id
