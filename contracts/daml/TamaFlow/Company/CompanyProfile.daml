module TamaFlow.Company.CompanyProfile where

-- | Company profile stored on-chain. Created via CompanyRegistry.
template CompanyProfile
  with
    employer : Party          -- Registry admin (signatory)
    employerName : Text       -- Real employer display name
    companyName : Text
    country : Text
  where
    signatory employer
