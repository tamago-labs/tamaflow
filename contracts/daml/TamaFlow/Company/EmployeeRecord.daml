module TamaFlow.Company.EmployeeRecord where

-- | Employee record linking an employee to a company. Employer creates, employee observes.
template EmployeeRecord
  with
    employer : Party
    employee : Party
    companyName : Text
    displayName : Text
    role : Optional Text
    createdAt : Time
  where
    signatory employer
    observer employee
