import { MessageSquare } from 'lucide-react'
import PlaceholderPage from './PlaceholderPage'

export function ChatPage() {
  return (
    <PlaceholderPage
      title='Chat'
      description='Group chat with your team, pinned to this workspace. Voice + file share land in v2.'
      icon={MessageSquare}
    />
  )
}

export default ChatPage
